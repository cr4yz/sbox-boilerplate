content_src is where uncompiled assets go.  It might follow the same hierarchy as the content folder.

For example, create your map under content_src/maps/yourmap.vmap and compiling will move it to the /content/maps folder automatically.
